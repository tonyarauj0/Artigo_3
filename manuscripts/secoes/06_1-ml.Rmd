## Número de Dimensões e Métricas de Avaliação

Cada votação pode representar uma questão ou uma temática e consequentemente pode ser uma dimensão no espaço político. Uma das análises permitidas pela metodologia utilizada é averiguar quantas dimensões explicam a maioria das votações da legislatura em questão. Uma das formas de fazer isso é através da variância total explicada pelos autovalores das matrizes de concordância do W-NOMINATE e da OC e pelo método de ACP.

Na Figura \@ref(fig:geralscree) tem-se um comparativo para os três métodos. Para a ACP, pode-se selecionar as CP cuja variância explicada acumulada é maior que 70%, ou escolher aquelas cujos autovalores são maiores ou iguais à unidade. Nesta pesquisa, serão selecionadas as dimensões até que o ganho de adicionar mais uma seja muito pequeno. Graficamente, busca-se um "cotovelo". Tal procedimento também será adotado para os demais métodos.

O "cotovelo" ocorre na passagem da primeira para a segunda dimensão, nos métodos W-NOMINATE e OC, e da segunda para a terceira na ACP. Pode-se inferir, por este método, uma baixa dimensionalidade da 55ª Legislatura. Essa conclusão, de apenas uma ou duas dimensões, é encontrada para o legislativo de outros países. @feliu2013tecnicas chegou a esta conclusão para o Paraguai, @poole2001d para o caso Norte-Americano e @leoni2002ideologia para a 49ª e a 50ª legislatura da Câmara dos Deputados do Brasil.

<br>

```{r, geralscreeplot, fig.cap= "Comparativo: Variância Explicada x Dimensões", fig.id = "geralscree", results='markup'}
knitr::include_graphics(path = here::here("figures", "resultados", "pontos_ideais", "geral_screeplot.png"))


```

<br>

Outras medidas que os pesquisadores utilizam para calcular a dimensionalidade são a Classificação Correta (CC) e a Redução Proporcional do Erro Agregado (APRE). Ambas são apresentadas na \@ref(tab:tbmetricas), para os modelos unidimensionais e bidimensionais.


<br>

```{r carregar dados metricas}
load(here::here("data","clean","df_metricas_metodos.Rda"))
```

<br>


```{r, tabela metricas, tab.id = "tbmetricas", tab.cap = "Classificação Correta e APRE, em percentuais, com até duas dimensões."}
df_metricas |> 
  flextable::flextable() |> 
  flextable::fontsize(part = "footer", size = 10) |> 
  flextable::align(align = "center", part = "body") |> 
  flextable::align(align = "center", part = "header") |> 
  flextable::hline_top(border = officer::fp_border(width = 1), part = "all") |> 
  flextable::hline_bottom(border = officer::fp_border(width = 1), part = "all") |> 
  flextable::set_header_labels(cc = "CC",
                               cc_1d = "CC\n 1D",
                               cc_2d = "CC\n 2D",
                               apre = "APRE",
                               apre_1d = "APRE 1D",
                               apre_2d = "APRE 2D",
                               metodo = "Método")


```

<br>

Embora não haja um incremento grande na CC com a inclusão de mais uma dimensão, a APRE, principalmente para o modelo de ACP, aumenta bastante. Portanto, conclui-se que os modelos com duas dimensões performam melhor. Dentre todos, é o OC que tem as medidas mais elevadas. Nota-se também que a ACP parece necessitar mais de uma terceira dimensão. Como não existe uma regra estatística para definir a adequação dos modelos, pode ser feita uma comparação com os demais trabalhos que utilizaram metodologia semelhante. É importante salientar que os objetos de estudo e períodos são distintos.  

Em seus modelos bidimensionais, @leoni2002ideologia encontrou uma CC de 88,6% e 91,2%  e APRE de 60,2% e 67,8%, para a Câmara dos Deputados, nas 49ª e 50ª legislaturas, respectivamente. @izumi2016governo, calculou a CC de modelos com duas dimensões para o Senado do Brasil da  48ª a 53ª legislatura, com valores que orbitavam entre 93,2% e 98,5%. Para a APRE, os limites foram 66% e 94%. Por fim, tem-se @leite2016analise que estudaram a Câmara Federal (2007-2010, 2011-2014), a estadual de São Paulo(2013-2016) e o Senado(2007-2014). Neste caso, a CC ficou entre 89% e 97%, enquanto a APRE entre 37% e 84%. Assim, os resultados obtidos para a 55ª legislatura estão dentro dos intervalos das demais pesquisas que utilizaram o Brasil como objeto de estudo.



## Distribuição dos Pontos Ideais

Nos mapas ilustrados a seguir, cada ponto indica a posição ideal de um parlamentar. Optou-se por, em um primeiro momento, diferenciá-los conforme o alinhamento com a coligação governista. Dessa forma, a visualização fica mais limpa, afinal são 32 partidos que possuíam pelo menos um representante em todo o período.

Além disso, embora existam valores para os eixos das dimensões, uma comparação entre os métodos não deve ser feita. As posições relativas devem ser levadas em consideração. Para melhor comparabilidade, os valores da primeira dimensão, para o W-NOMINATE e a OC, foram multiplicados por -1.

A Figura \@ref(fig:wnominatepontos) exibe a distribuição dos pontos ideais calculados pelo W-NOMINATE e a Figura \@ref(fig:ocpontos) mostra o mapa construído pela OC. A interpretação de ambos é semelhante. Existe uma concentração de pontos no centro, os Deputados da Oposição parecem se concentrar mais abaixo do ponto zero da segunda dimensão e os pontos no extremo esquerdo do 4º quadrante são, em maioria, políticos do PT.

<br>


```{r, wnominatepontos, fig.cap= "Mapa Espacial de Votações, W-NOMINATE", fig.id = "wnominatepontos", results='markup'}
knitr::include_graphics(path = here::here("figures", "resultados", "pontos_ideais", "grafico_wnominate_pontos.png"))


```

<br>

```{r, ocpontos, fig.cap= "Mapa Espacial de Votações, OC", fig.id = "ocpontos", results='markup'}
knitr::include_graphics(path = here::here("figures", "resultados", "pontos_ideais", "grafico_oc_pontos.png"))


```

<br>

Por fim, em \@ref(fig:pcapontos) tem-se o mapa formado a partir da ACP. Essa transformação não prejudica a análise, apenas deixa que os métodos tenha a mesma "polaridade". Neste mapa, fica mais evidente a separação gráfica entre Governo e Oposição dada pela dimensão 2. Os parlamentares do PT continuam ocupando a mesma posição relativa que os gráficos anteriores.

<br>

```{r, pcapontos, fig.cap= "Mapa Espacial de Votações, ACP", fig.id = "pcapontos", results='markup'}
knitr::include_graphics(path = here::here("figures", "resultados", "pontos_ideais", "grafico_radar_pca_pontos.png"))


```

<br>

A fim de averiguar se os três métodos configuram o mesmo cenário político calcularam-se as correlações entre as coordenadas dos mesmos, representadas na Figura \@ref(fig:cord1). Observa-se uma alta correlação, significantes a 1%, dos pontos da primeira dimensão, principalmente para o W-NOMINATE e OC. Além disso, os sinais são invertidos em relação ao ACP. Outro ponto de destaque são as duas grandes concentrações nas distribuições para W-NOMINATE e OC, e em três pontos para a ACP.

<br>

```{r, correlacaod1, fig.cap= "Distribuição e Correlação entre os Métodos, 1ª Dimensão", fig.id = "cord1", results='markup'}
knitr::include_graphics(path = here::here("figures", "resultados", "pontos_ideais", "correlacao_d1.png"))


```

<br>

Quanto à segunda dimensão, as correlações são altas e significantes, ligeiramente menores, mas todas positivas. Outro destaque a ser feito é que as distribuições dos pontos são unimodais. Isso pode ser observado na Figura \@ref(fig:cord2).

<br>


```{r, correlacaod2, fig.cap= "Distribuição e Correlação entre os Métodos, 2ª dimensão", fig.id = "cord2", results='markup'}
knitr::include_graphics(path = here::here("figures", "resultados", "pontos_ideais", "correlacao_d2.png"))


```

<br>

Uma vez apresentados os pontos ideais para cada parlamentar, calculou-se a mediana destes por partido. A ideia é ter a representação do Eleitor Mediano em cada legenda. Essa visualização, nas figuras seguintes, é importante porque permite comparar a posição dos partidos. Outras possibilidades seriam calcular a média dos pontos ideais em cada dimensão para cada partido ou até menos representá-los pelas preferências de seus líderes. Neste último caso há um risco de se assumir que todos os políticos de um partido tem a mesma preferência que seu líder. Um último destaque a ser feito é a posição oposta, em ambas dimensões, do PT e do PSDB.


```{r, wnominatemed, fig.cap= "Mapa espacial de votações, W-NOMINATE (Eleitor Mediano). ", fig.id = "wmediano", results='markup'}
knitr::include_graphics(path = here::here("figures", "resultados", "pontos_ideais", "grafico_wnominate_mediano.png"))


```

  

```{r, ocmed, fig.cap= "Mapa espacial de votações, OC (Eleitor Mediano). ", fig.id = "ocmediano", results='markup'}
knitr::include_graphics(path = here::here("figures", "resultados", "pontos_ideais", "grafico_oc_mediano.png"))


```

  

```{r, acpmed, fig.cap= "Mapa espacial de votações, ACP (Eleitor Mediano). ", fig.id = "acpmediano", results='markup'}
knitr::include_graphics(path = here::here("figures", "resultados", "pontos_ideais", "grafico_acp_mediano.png"))


```





